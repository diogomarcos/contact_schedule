<div class="basic-grey">
<p id="notice"><%= notice %></p>

<h1><%= "#{t('messages.show')} #{@phone.model_name.human(count: 1)}" %></h1>

<p>
  <strong><%= @phone.class.human_attribute_name("phone") %>:</strong>
  <%= @phone.phone %>
</p>

<p>
  <strong><%= @phone.class.human_attribute_name("contact") %>:</strong>
  <%= @phone.contact.name %>
</p>

<%= link_to t('buttons.edit'), edit_phone_path(@phone) %> |
<%= link_to t('buttons.back'), phones_path %>
</div>

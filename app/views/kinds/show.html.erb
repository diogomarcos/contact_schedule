<div class="basic-grey">
<p id="notice"><%= notice %></p>

<h1><%= "#{t('messages.show')} #{@kind.model_name.human(count: 1)}" %></h1>

<p>
  <strong><%= @kind.class.human_attribute_name("description") %>:</strong>
  <%= @kind.description %>
</p>

<%= link_to t('buttons.edit'), edit_kind_path(@kind) %> |
<%= link_to t('buttons.back'), kinds_path %>
</div>
